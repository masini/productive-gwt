<module>
	<inherits name="org.googlecode.gwt.base.Base"/>
	<inherits name='org.googlecode.gwt.template.Template'/>
	
	<stylesheet src='css/header.css'/>
	
	
	
	<!-- Browser-sensitive code should use the 'user.agent' property -->
    <define-property name="header.pdauseragent" values="ie6pda,other" />
    <property-provider name="header.pdauseragent"><![CDATA[
      var ua = navigator.userAgent.toLowerCase();
      var makeVersion = function(result) {
          return (parseInt(result[1]) * 1000) + parseInt(result[2]);
      };
        if (ua.indexOf("msie") != -1) {
        var result = /msie ([0-9]+)\.([0-9]+)/.exec(ua);
            if (result && result.length == 3) {
                if (makeVersion(result) >= 6000) {
                    if(ua.indexOf("windows ce")!=-1){
                        return "ie6pda";
                    }
                }
            } 
        }
      return "other";
    ]]></property-provider>   
    
    
     <!--  Deferred binding of TemplateManager -->

    <replace-with class="org.googlecode.gwt.header.client.HeaderPDA">
        <when-type-is class="org.googlecode.gwt.header.client.HeaderImpl" />
        <when-property-is name="header.pdauseragent" value="ie6pda" />
    </replace-with>
    
    <replace-with class="org.googlecode.gwt.header.client.HeaderNOPDA">
        <when-type-is class="org.googlecode.gwt.header.client.HeaderImpl" />
        <when-property-is name="header.pdauseragent" value="other" />
    </replace-with>
	
	
	<entry-point class="org.googlecode.gwt.header.client.Header"/>
</module>
